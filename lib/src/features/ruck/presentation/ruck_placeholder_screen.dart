import 'package:flutter/material.dart';

class RuckPlaceholderScreen extends StatelessWidget {
  const RuckPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Ruck module placeholder'),
      ),
    );
  }
}
