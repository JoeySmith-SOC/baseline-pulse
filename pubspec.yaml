name: baseline_pulse
description: BASELINE PULSE tactical run + ruck platform.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.8.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  geolocator: ^13.0.2
  google_maps_flutter: ^2.12.1
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  intl: ^0.20.2
  uuid: ^4.5.1
  purchases_flutter: ^8.7.0
  health: ^13.1.4
  share_plus: ^10.1.4
  flutter_svg: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
